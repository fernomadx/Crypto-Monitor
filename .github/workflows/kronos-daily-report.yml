# Relatório diário Kronos = cron Railway (vps/kronos_daily_report.py).
# Este workflow não envia Telegram.

name: Kronos Daily Report

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Kronos só no Railway
        run: |
          echo "Relatório diário Kronos só no Railway crontab (15:00 UTC)."
          echo "Não enviar a partir do GitHub Actions."

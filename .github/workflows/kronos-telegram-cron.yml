# DESATIVADO: Kronos paper só no Railway (daemon + crontab).
# Não disparar sinal daqui — seria uma terceira casa (além de Hetzner).

name: Kronos Telegram Signal

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Kronos só no Railway
        run: |
          echo "Kronos paper só corre no Railway."
          echo "Não rode sinal/scorecard no GitHub Actions."
          echo "Hetzner: bash vps/hetzner_disable_kronos.sh"

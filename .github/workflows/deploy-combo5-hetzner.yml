# DESATIVADO: COMBO5 na Hetzner 204 é atualizado no boot Railway (heal_all).
# O secret VPS_SSH_KEY desta Action está morto — runs #1–#6 falham em ~8s no SSH.
# Não disparar no push (evita X vermelho em cada merge de quant_bot.py).

name: Deploy COMBO5 to Hetzner

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy é o Railway
        run: |
          echo "COMBO5 / heal Hetzner corre no boot Railway (vps/railway_boot.sh → heal_all)."
          echo "Não usar este workflow — o secret VPS_SSH_KEY falha em Authentication failed."
          echo "Telegram: /vps test   ·   204 já sync no último boot."
